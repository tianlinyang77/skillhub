# Plugin definitions

Reviewed source definitions for installable plugin bundles belong here. Each
definition must select explicit published skills and declare any tool or MCP
dependencies. It must not include `staging/` or discover skills recursively.

No production plugin is defined yet. Add the first manifest together with its
builder, validation test and generated output contract.
