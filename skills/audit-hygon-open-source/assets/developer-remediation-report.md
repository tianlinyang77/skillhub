# <repo-id> HYGON 开源合规整改清单

## 1. 扫描结论

- 私人仓库分支和 Commit
- 上游仓库、分支、Tag、Commit、许可证
- 普通 fork 模式列出新增、修改、删除、重命名、类型变化的加总关系
- submodule-patch 模式列出新增、同路径修改、类型变化、同路径上游原样、固定上游 Gitlink 的加总关系，并明确上游仅存在于 submodule 的路径不属于删除
- upstream-overlay 模式列出新增、同路径修改、类型变化、同路径上游原样的加总关系，并明确上游仅有路径不属于删除
- 已确认必须整改、目前仅需核对、无阻断三类互斥文件数量
- 已确认必须整改拆分为整改即可、整改并核对，两类之和必须等于必须整改文件数
- 三类顶层文件数量之和必须等于差异文件总数
- 已确认 HYGON 新增/修改源码的头文件完成情况，完整与不完整之和必须等于统计范围
- 已完成头文件拆分为 HYGON 新增和 HYGON 修改，两类之和必须等于已完成数量
- 明确声明平台命名及运行时文案不在本报告范围，需单独执行平台专项 Skill
- 最终准入结论

### 文件头整改模板（统一引用）

- 在本区域集中给出一次完整模板，后续逐文件清单不得重复粘贴
- 使用稳定模板编号，至少覆盖：HYGON 原创/新增、二次开发实质性原创修改、二次开发非实质修改、恢复原声明、第三方源码、生成文件
- 说明 `#` 和 `//` 的适用文件，统一用 `#` 展示内容，并说明 slash-comment 文件替换注释符、shebang 保持第一行
- 许可证已确认时，模板使用真实且精确的 SPDX 标识
- 许可证未确认时，不替开发选择协议；列出 MIT、BSD-3-Clause、Apache-2.0 的精确 SPDX 映射，模板保留占位符
- 明确上游/第三方原声明必须保留；第三方原文件不直接补 SPDX；vendor、第三方、生成文件、JSON 和 lock 文件不得机械补 HYGON 头

## 2. 不支持许可证阻断项

列出非 MIT/BSD-3-Clause/Apache-2.0、未知、缺失和复合许可证。

## 3. 必须修改的文件及修改内容

逐文件说明当前问题、保留内容、需要增加或替换的内容。涉及文件头时，每个文件只引用一次统一模板编号；同一文件同时缺 Copyright、SPDX 或 Apache 修改说明时合并引用，不得重复完整头文件。不得给开发提供扫描命令。

## 4. 来源核对清单

只列证据确实不足、无法确定修改性质或许可证的文件，并说明确认后采用的分支处置和对应统一模板编号。目标差异行已经全部归属于私仓独有提交的上游修改文件，不得再次要求开发确认来源，应直接进入必须修改或无需修改清单。区分 HYGON 实质性原创修改和非实质修改：Apache-2.0 的 H2 追加 Copyright、SPDX 和修改说明，H3 仅加修改说明；MIT/BSD 的 H2/H3 文件头保持不变，H2 改为仓库级贡献登记。平台运行可见性不在本报告核对。

自动解析失败必须单独写明失败的解析器或语义比较方法。不得仅写“待确认”。已证明的上游部分回移必须记录唯一上游 Commit，并放入无需修改清单。

## 5. 无需修改清单

列出 R100、上游相同 blob 和其他明确排除项及原因。

## 6. Commit log 建议

仅列建议，不作为开源阻断项。

## 7. 开发提交核对清单

列出交付前必须完成的内容，不包含审计过程。
